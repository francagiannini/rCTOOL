% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Temperature.R
\name{set_monthly_temperature_data_historical_amplitude}
\alias{set_monthly_temperature_data_historical_amplitude}
\title{set_monthly_temperature_data_historical_amplitude}
\usage{
set_monthly_temperature_data_historical_amplitude(file = NULL, Tavg = NULL)
}
\arguments{
\item{file}{Either a filepath to a csv file or a data.frame containing
monthly temperature data.}

\item{Tavg}{Numeric vector of average monthly temperature values.}
}
\value{
A data.frame containing `Tavg` and a single historical
  `Amplitude` repeated over the full series.
}
\description{
Prepare monthly temperature input with a single historical amplitude.
}
\details{
Monthly `Tavg` data can be provided either as a filepath/data.frame
through `file` or directly as a numeric vector through `Tavg`.

A single historical amplitude is calculated from the full monthly
`Tavg` series as:
\deqn{(max(Tavg) - min(Tavg)) / 2}

This historical amplitude is then assigned to the full series in the
`Amplitude` column.
}
\examples{
set_monthly_temperature_data_historical_amplitude(
  Tavg = c(1, 2, 4, 7, 11, 15, 17, 16, 13, 9, 5, 2)
)
}
